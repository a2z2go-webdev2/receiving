# Coding Standard

PHP:

```bash
composer lint
composer analyse
php artisan test
```

TypeScript:

```bash
npm run check
npm run typecheck
npm run build
```

Prefer existing framework conventions, Form Requests for validation, policies/permissions for authorization, and small actions/services for reusable business behavior.
